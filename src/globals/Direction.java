package globals;

/**
 *      Direction is the class containing the enum for game Directions and there respected values
 *      @author Wayne A. Campbell
 *      @version 1.2 - 4/8/2015
 */
public enum Direction {
    NORTH,
    SOUTH,
    EAST,
    WEST;
    
    
    public static final int NOEXIT = -1; //Keeping track of the no exits by setting them as =1;
}
